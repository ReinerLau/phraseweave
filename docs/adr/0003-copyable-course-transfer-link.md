# 二维码与可复制链接共用课程传输会话

课程同步继续保留二维码，同时增加可复制的同步链接；两者都只用于加入当前课程传输会话，不引入永久分享或云端课程存储。这样可以覆盖两台设备不在一起时的同步场景，并复用现有传输会话的生命周期：发送页面关闭或传输完成后，入口失效。

## Considered Options

- 只保留二维码：两台设备不在一起时无法使用。
- 引入永久课程分享链接：超出当前同步需求，并增加存储、权限和生命周期复杂度。
- 采用二维码与临时同步链接并存：覆盖现场扫码和远距离复制两种入口，同时保持现有传输边界。
