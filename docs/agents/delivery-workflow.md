# 手动迭代流程

本仓库不强制固定的交付节奏、指令或记录方式。由用户根据当前工作主动发起下一步，下面的顺序只作为参考。

1. 明确目标，并在需要时先整理简短计划。
2. 按计划实现改动。
3. 根据改动风险和当前环境，按需运行检查、测试或构建。
4. 按需提交、创建分支、使用 Worktree、创建 Issue 或 Pull Request。
5. 需要发布时，手动触发 GitHub Pages 的生产部署。

Issue、状态标签、分支目标、Pull Request、自动检查和验收记录都不是流程门槛。若某次工作需要更严格的记录或检查，可以在当次工作中由用户明确指定。
